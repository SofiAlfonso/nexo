# API del coordinador

Expone V1 `POST /v1/validaciones` y H1 `POST /v1/heartbeats` y `POST /v1/diario/lotes`.
No debe implementar reglas de dominio ni acceder directamente a la base de datos.
Se completa en la ola 1.
