# Simulador de boletería

Simula el sistema externo de boletería para pruebas; no forma parte de C4.
Expone P1 `GET /versiones` y `GET /versiones/{n}`.
Se ejecuta en el namespace `nexo-external` y no debe contener lógica interna del núcleo central.
Se completa en la ola 1.
