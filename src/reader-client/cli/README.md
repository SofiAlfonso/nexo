# CLI del lector

Permite ejecutar el lector emulado y sus perfiles de generación de carga.
No debe duplicar reglas de acceso; los perfiles de carga se definen en `tests/load/`.
Se completa en la ola 1.
